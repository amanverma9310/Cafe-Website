# Backend API base URL (must end with /api). Local backend default shown.
VITE_API_URL=http://localhost:5000/api

# Public URL of this website — used for canonical links, sitemap.xml and robots.txt at build time.
VITE_SITE_URL=http://localhost:5173
